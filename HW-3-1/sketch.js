function setup() {
  createCanvas(600, 800);
  background(207, 238, 242);
}

function draw() {
  
  // paint tool
  noStroke();
  fill(259, 200, 200);
  ellipse(mouseX, mouseY, 5, 5);
}

//function mouseIsPressed () {
  //background(207, 238, 242);
//} 